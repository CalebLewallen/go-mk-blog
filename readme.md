# Caleb Lewallen Personal Blog

Wanted to learn more about Go and the embeded package, so decided to start migrating a blog here.

It's a basic Go + Templ + Markdown stack, with directory bassed routing.

Simple to run, just clone the repo and

```bash
go run main.go
```